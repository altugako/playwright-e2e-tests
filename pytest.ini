[pytest]
# directory where tests are located
testpaths = tests

# options to pytest; -v for verbose output is a common default
addopts = -v

# test markers (optional, but helps avoid warnings)
markers =
    e2e: end-to-end tests using Playwright

# asyncio mode for pytest-asyncio: auto avoids nested event loop errors
asyncio_mode = auto
